{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <div class="row">
        <div class="col-sm-6">
            <h1 class="pull-left">Crear ticket</h1>
        </div>
        <div class="col-sm-6">
            <a class="pull-right btn btn-danger" href="{{ path('ticket_index') }}">Cancelar</a>
        </div>
    </div>

    {{ form_start(form) }}

    {{ form_widget(form) }}

        <input class="btn btn-primary" type="submit" value="Crear" />

    {{ form_end(form) }}



{% endblock %}
